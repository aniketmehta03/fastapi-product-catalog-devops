apiVersion: v1
kind: Service
metadata:
  name: mongo
  labels:
    {{- include "product-api.labels" . | nindent 4 }}
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017