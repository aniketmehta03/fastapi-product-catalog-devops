apiVersion: v1
kind: Secret
metadata:
  name: {{ include "product-api.fullname" . }}-secret
  labels:
    {{- include "product-api.labels" . | nindent 4 }}
type: Opaque
data:
  DATABASE_URL: {{ .Values.database.url | b64enc | quote }}